#ifndef TYPES_H
#define TYPES_H

#include <stdint.h>
#include <stddef.h>

typedef unsigned long   word_t;   // machine word
typedef unsigned long   addr_t;   // address
typedef uint64_t        paddr_t;  // physical address
typedef uint64_t        psize_t;  // physical address size

#endif // TYPES_H
